import { CDirDirective } from './c-dir.directive';

describe('CDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CDirDirective();
    expect(directive).toBeTruthy();
  });
});
